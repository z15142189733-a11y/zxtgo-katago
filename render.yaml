services:
  - type: web
    name: zxtgo-katago
    env: docker              # ← Docker 服务必须用 env: docker（不是 runtime:）
    region: singapore        # 新加坡节点，亚洲延迟最低
    plan: free               # 免费计划（15分钟无访问后自动休眠）
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "10000"
