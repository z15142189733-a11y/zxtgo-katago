services:
  - type: web
    name: zxtgo-katago
    runtime: docker          # ← 改为 Docker，可直接 apt-get 安装 libzip5
    region: singapore        # 新加坡节点，亚洲延迟最低
    plan: free               # 免费计划（15分钟无访问后自动休眠）
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "10000"
